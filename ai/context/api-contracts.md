# API Contracts (AI Context)

This document gives AI agents context on REST conventions, HTTP verb usage, the response envelope shape, and the authentication header format expected across CultureLinkCRM's API.

## REST Conventions

TODO: fill this in — document resource naming, pluralization, and URL structure conventions once endpoints are designed.

## HTTP Verb Usage

TODO: fill this in — document expected use of GET/POST/PUT/PATCH/DELETE and idempotency expectations.

## Response Envelope Shape and Auth Header Format

TODO: fill this in — document the standard JSON response envelope (success/error shape) and the expected `Authorization` header format.
