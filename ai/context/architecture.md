# Architecture (AI Context)

This document gives AI agents a quick-reference summary of layer responsibilities, project dependency direction, and hard rules that must never be violated when working in this codebase.

## Layer Responsibilities

TODO: fill this in — summarize what Core, Infrastructure, Api, and Client are each responsible for, once they exist under `source/`.

## Project Dependency Direction

TODO: fill this in — state explicitly which projects may reference which (e.g. Api → Core, Api → Infrastructure, Infrastructure → Core, Client → Api).

## What Agents Should Never Do

TODO: fill this in — e.g. never reference Infrastructure from Core, never put business logic in controllers, never bypass the service interface layer.
