# Data Model (AI Context)

This document gives AI agents context on CultureLinkCRM's intended domain entities, how Entity Framework Core is used, and the strategy for evolving the schema over time.

## Key Domain Entities (Placeholder Names)

TODO: fill this in — list placeholder entity names (e.g. Organization, Contact, Interaction, User) once the domain model is defined.

## EF Core Usage

TODO: fill this in — describe conventions for DbContext design, entity configuration (Fluent API vs. attributes), and repository/service patterns.

## Migration Strategy

TODO: fill this in — document how EF Core migrations will be created, reviewed, and applied across environments, and how they relate to `database/migrations/`.
