# Deployment (AI Context)

This document gives AI agents context on the intended Docker setup, environment variable names, SQL Server connection string pattern, and secrets handling for CultureLinkCRM.

## Docker Setup

TODO: fill this in — describe the intended multi-stage Dockerfile and docker-compose services once implemented, referencing `build/Dockerfile` and `build/docker-compose.yml`.

## Environment Variable Names

TODO: fill this in — list the expected environment variable names (e.g. connection strings, feature flags) once defined.

## SQL Server Connection String Pattern and Secrets Handling

TODO: fill this in — document the expected connection string format and how secrets (e.g. via user-secrets locally, environment variables or a secrets manager in deployed environments) are handled.
