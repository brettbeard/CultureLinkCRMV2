# Conventions

This document defines the C# coding standards, naming conventions, async/await rules, and dependency injection patterns that AI agents and developers must follow when writing code in CultureLinkCRM, including which patterns are preferred versus banned.

## C# Coding Standards and Naming Conventions

TODO: fill this in — document naming rules (PascalCase for public members, camelCase for locals/parameters, `_camelCase` for private fields, etc.) and file-scoped namespace usage.

## Async/Await Rules

TODO: fill this in — document that all I/O-bound code must be async, naming suffix conventions (`Async`), and cancellation token propagation expectations.

## DI Patterns: Preferred vs. Banned

TODO: fill this in — document constructor-only DI, banned patterns (service locator, static state, `new`-ing up services directly), and preferred lifetime scopes (scoped/transient/singleton usage).
