# Onboarding

This document explains how a new developer sets up CultureLinkCRM locally, from prerequisites through running the application and its test suite.

## Prerequisites

TODO: fill this in — list required tooling (e.g. .NET 10 SDK, SQL Server or Docker, IDE recommendations) and versions.

## Clone and Local Setup

TODO: fill this in — describe how to clone the repository, restore dependencies, and configure local settings/secrets once the solution exists in `source/`.

## Running the App and Tests

TODO: fill this in — document the commands to run the application locally and to execute the xUnit test suite.
