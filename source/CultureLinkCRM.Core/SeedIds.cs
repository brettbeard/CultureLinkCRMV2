namespace CultureLinkCRM.Core;

/// <summary>Well-known primary keys for seeded, system-managed rows (computed Segments), so code can reference them without a name lookup.</summary>
public static class SeedIds
{
    public const int DonorActiveSegmentId = 1;
    public const int DonorLapsedSegmentId = 2;
}
